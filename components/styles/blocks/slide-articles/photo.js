import styled from "styled-components";

const Photo = styled.figure`
    width: 2.5rem;
    height: 2.5rem;
    overflow: hidden;
    border-radius: 50%;
    margin-right: .5rem;
    img {
        width: 100%;
    }
`;

export default Photo;