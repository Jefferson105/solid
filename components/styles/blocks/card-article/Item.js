import styled from "styled-components";

const Item = styled.li`
    box-shadow: 0 0 30px 1px rgba(0,0,0,.2);
    height: 20rem;
    display: flex;
    border-radius: 15px;
    margin-bottom: 2rem;
`;

export default Item;